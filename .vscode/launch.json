{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch with create flag",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "args": ["--get"],
            "cwd": "${workspaceFolder}",
            "showLog": true,
            "console": "integratedTerminal"
        }
    ]
}